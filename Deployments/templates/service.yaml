apiVersion: v1
kind: Service
metadata:
  name: {{ include "boards-service.fullname" . }}
  labels:
    {{- include "boards-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "boards-service.selectorLabels" . | nindent 4 }}